// mod collinearity;
// use collinearity::{HeapSort, BinaryHeapSort, BruteCollinearPoints, Point};

// mod calculator;
// use calculator::{BinaryHeapPriorityQueue, PriorityQueueKind};
fn main() {
    // let mut h = HeapSort::<isize>::init(vec![-1, -10, 16, 37, -90, 13, 8, 0, 17]);
    // let v = h.into_sorted_vec();
    // println!("{:?}", v);
}
